import { FavoritesList } from "@/features/favorites";

// Favorite organizations. See R8. Thin route — delegates to the feature screen.
export default function FavoritesScreen(): React.JSX.Element {
	return <FavoritesList />;
}
