/*
 * Public surface of the display feature. Routes import from here rather than
 * reaching into internals.
 */
export { DisplayBoard } from './components/DisplayBoard';
export type { DisplayBoardProps } from './components/DisplayBoard';
